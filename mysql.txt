This conversation was generated with Duck.ai (https://duck.ai) using OpenAI's GPT-4o mini Model. AI chats may display inaccurate or offensive information (see https://duckduckgo.com/duckai/privacy-terms for more info).

====================

User prompt 1 of 1 - 9/20/2025, 11:15:01 PM:
write a mysql chet sheet all commands

GPT-4o mini:
Here's a comprehensive MySQL cheat sheet that covers essential commands and concepts. This cheat sheet is organized into sections for easy reference.

## MySQL Cheat Sheet

### Database Commands

|-----------------------------|--------------------------------------------------|
| Command                     | Description                                      |
|-----------------------------|--------------------------------------------------|
| `CREATE DATABASE db-name;`  | Creates a new database.                          |
| `DROP DATABASE db-name;`    | Deletes a database and all its tables.         	 |
| `USE db-name;`              | Selects a database to work with.                 |
| `SHOW DATABASES;`           | Lists all databases on the server.             	 |
|-----------------------------|--------------------------------------------------|

---

### Table Commands

|----------------------------------------------------------------------|--------------------------------------------------|
| Command                                                              | Description                                      |
|----------------------------------------------------------------------|--------------------------------------------------|
| `CREATE TABLE table-name (column1 datatype, column2 datatype, ...);` | Creates a new table.                             |
| `DROP TABLE table-name;`          				                   | Deletes a table.                                 |
| `ALTER TABLE table-name ADD column-name datatype;`		           | Adds a new column to a table. 			          |
| `ALTER TABLE table-name DROP column-name;`			               | Removes a column from a table.			          |
| `SHOW TABLES;`                    				                   | Lists all tables in the selected database.       |
| `DESCRIBE table-name;`                                               | Shows the structure of a table.                  |
|----------------------------------------------------------------------|--------------------------------------------------|
---

### Data Manipulation Commands

|--------------------------------------------------------------------------------|--------------------------------------------------|
| Command                                                                        | Description                                      |
|--------------------------------------------------------------------------------|--------------------------------------------------|
| `INSERT INTO table-name (column1, column2, ...) VALUES (value1, value2, ...);` | Inserts new data into a table.                   |
| `SELECT * FROM table-name;`                                                    | Retrieves all records from a table.              |
| `SELECT column1, column2 FROM table-name WHERE condition;`                     | Retrieves specific columns based on a condition. |
| `UPDATE table-name SET column1 = value1 WHERE condition;`                      | Updates existing records in a table.             |
| `DELETE FROM table-name WHERE condition;`                                      | Deletes records from a table.                    |
|--------------------------------------------------------------------------------|--------------------------------------------------|

---

### Query Clauses

|------------------------------------|-------------------------------------------------------------|
| Command                            | Description                                                 |
|------------------------------------|-------------------------------------------------------------|
| `WHERE condition;`                 | Filters records based on a condition.                       |
| `ORDER BY column ASC|DESC;`        | Sorts the result set by one or more columns.                |
| `GROUP BY column;`                 | Groups rows that have the same values in specified columns. |
| `HAVING condition;`                | Filters groups based on a condition.                        |
| `LIMIT number;`                    | Limits the number of records returned.                      |
|------------------------------------|-------------------------------------------------------------|

---

### Indexing Commands

|--------------------------------------------------------|--------------------------------|
| Command                                                | Description                    |
|--------------------------------------------------------|--------------------------------|
| `CREATE INDEX index-name ON table-name (column-name);` | Creates an index on a table.   |
| `DROP INDEX index-name ON table-name;`                 | Deletes an index from a table. |
|--------------------------------------------------------|--------------------------------|

---

### User Management Commands

|--------------------------------------------------------------|-------------------------------------|
| Command                                                      | Description                         |
|--------------------------------------------------------------|-------------------------------------|
| `CREATE USER 'username'@'host' IDENTIFIED BY 'password';`    | Creates a new user.                 |
| `DROP USER 'username'@'host';`                               | Deletes a user.                     |
| `GRANT ALL PRIVILEGES ON db-name.* TO 'username'@'host';`    | Grants privileges to a user.        |
| `REVOKE ALL PRIVILEGES ON db-name.* FROM 'username'@'host';` | Revokes privileges from a user.     |
| `SHOW GRANTS FOR 'username'@'host';`                         | Shows privileges granted to a user. |
|--------------------------------------------------------------|-------------------------------------|

---

### Transaction Commands

|-------------------------------------|--------------------------------------------------|
| Command                             | Description                                      |
|-------------------------------------|--------------------------------------------------|
| `START TRANSACTION;`                | Begins a new transaction.                        |
| `COMMIT;`                           | Saves all changes made in the transaction.       |
| `ROLLBACK;`                         | Undoes changes made in the transaction.          |
|-------------------------------------|--------------------------------------------------|

---

### Miscellaneous Commands

|-------------------------------------|--------------------------------------------------|
| Command                             | Description                                      |
|-------------------------------------|--------------------------------------------------|
| `SHOW PROCESSLIST;`                 | Shows currently running processes.               |
| `SHOW VARIABLES;`                   | Displays server system variables.                |
| `SHOW STATUS;`                      | Displays server status information.              |
|-------------------------------------|--------------------------------------------------|

### Common Data Types

|--------------------------|------------------------------------------------------------------------|
| Data Type                | Description                                                            |
|--------------------------|------------------------------------------------------------------------|
| `INT`                    | Integer value.                                                         |
| `INT AUTO_INCREMENT`     | Integer value that automatically increments with each new record.      |
| `BIGINT`                 | Larger integer value for bigger numbers.                               |
| `VARCHAR(n)`             | Variable-length string with a maximum length of n.                     |
| `CHAR(n)`                | Fixed-length string that is always n characters long.                  |
| `TEXT`                   | Large text string.                                                     |
| `DATE`                   | Date value (YYYY-MM-DD).                                               |
| `DATETIME`               | Date and time value.                                                   |
| `FLOAT`                  | Floating-point number.                                                 |
| `DOUBLE`                 | Double-precision floating-point number.                                |
| `DECIMAL(p, s)`          | Fixed-point number with p total digits and s digits after the decimal. |
| `BOOLEAN`                | Boolean value (TRUE or FALSE).                                         |
| `BLOB`                   | Binary Large Object for storing binary data.                           |
| `NOT NULL`               | Constraint that ensures a column cannot have a NULL value.             |
| `UNIQUE`                 | Constraint that ensures all values in a column are different.          |
| `DEFAULT`                | Sets a default value for a column if no value is specified.            |
| `PRIMARY KEY`            | Uniquely identifies each record in a table.                            |
|--------------------------|------------------------------------------------------------------------|

 